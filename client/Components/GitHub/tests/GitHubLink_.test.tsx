import * as React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom';


describe("Testing The Github Auth when a user trying to link their github to their profile", () => {
        test("Testing validating the Link", () => {
                
        })
})